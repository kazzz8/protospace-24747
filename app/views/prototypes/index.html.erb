<main class="main">
  <div class="inner">
    <%# ログインしているときは以下を表示する %>
    <% if user_signed_in? %>
      <div class="greeting">
        こんにちは、
        <%= link_to current_user.name , user_path(current_user.id), class: :greeting__link%>
        さん
      </div>
    <% end %>
    <div class="card__wrapper">
      <%= render partial: "prototype", collection: @prototypes %>
      <%# 部分テンプレートへ複数のprototypeの入った@prototypesを送っている。送った@prototypesの中身はひとつひとつはprototypeという変数で扱うことができる。多分より簡素に書けるようにするRailsの機能なのかな %>
    </div>
  </div>
</main>
